steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'gcr.io/unitebystep/step128-2020', '.']
- name: 'gcr.io/cloud-builders/curl'
  entrypoint: bash
  args: ['-c', 'bash <(curl -s https://codecov.io/bash)']
  env:
  - 'CODECOV_TOKEN=074ac000-bb84-4cd5-9dea-6cc6ebe78843' # _CODECOV_TOKEN is user user substituion variable specified in my cloud build trigger

images:
- 'gcr.io/unitebystep/step128-2020'